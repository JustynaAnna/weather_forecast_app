name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    env: 
      apiKey: ${{ secrets.MY_API_KEY }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14

      - name: Install dependencies
        run: npm install

      - name: Build and copy files
        run: |
          mkdir temp_public_html
          cp -r ./* temp_public_html
          mv temp_public_html/. ./
          rmdir temp_public_html
          cp -r ./.github public_html

      - name: Deploy to GitHub Pages
        run: |
          # Install the GitHub Pages deployment tool
          npm install -g gh-pages
           export apiKey=$MY_API_KEY

          # Deploy the project to GitHub Pages
          npx gh-pages -d build

